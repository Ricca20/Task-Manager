# Task Manager Frontend

React + Tailwind CSS frontend for the Task Manager application.

## Scripts

-   `npm run dev`: Start the development server
-   `npm run build`: Build for production
-   `npm run preview`: Preview the production build

## Configuration

-   **API URL**: Configured in `src/api/axios.js` (defaults to `http://localhost:3000/api`)
